<Window x:Class="TicTacToeSolver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TicTacToeSolver"
        mc:Ignorable="d"
        Title="MainWindow" SizeToContent="WidthAndHeight">
    <Window.DataContext>
        <local:MainVM/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Slider Value="{Binding BoardSize}" Minimum="3" Maximum="10" Width="300"/>
            <Label>
                <Label.Content>
                    <TextBlock>
                        <Run Text="Trained "/>
                        <Run Text="{Binding TimesTrained}"/>
                        <Run Text=" times"/>
                    </TextBlock>
                </Label.Content>
            </Label>
            <TextBox Width="400" Text="{Binding TrainingDepth}" />
            <Button Content="Play" Width="200" Command="{Binding PlayCommand}"/>
            <Button Content="Play Educated" Width="200" Command="{Binding PlayWhatWasLearnedCommand}"/>
        </StackPanel>
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Cells}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="{Binding BoardSize}"
                         Columns="{Binding BoardSize}" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button
                Content="{Binding Content}"
                Command="{Binding Command}"
                CommandParameter="{Binding}"
                Width="60"
                Height="60"
                FontSize="24"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Grid.Row="2" Content="Clear board" Command="{Binding DrawBoardCommand}"/>
    </Grid>
</Window>
